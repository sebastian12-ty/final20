# Local: SQLite se crea automáticamente - no requiere configuración
SECRET_KEY=edusatisface-secret-2024
FLASK_ENV=development

# Render (producción) - se configuran solos vía render.yaml, dejar de referencia:
# DB_PATH=/var/data/edusatisface.db     -> ruta en el Disk persistente
# DATABASE_URL=postgresql://...         -> si usas Postgres en vez de SQLite

